dev:
  host:
  user:
  uri:
  ssh:
    tty: false
    options: '-p 2222 -o "AddressFamily inet"'
  paths:
    files: files
    drush-script: drush9

stage:
  host:
  user:
  uri:
  ssh:
    tty: false
    options: '-p 2222 -o "AddressFamily inet"'
  paths:
    files: files
    drush-script: drush9

production:
  host:
  user:
  uri:
  ssh:
    tty: false
    options: '-p 2222 -o "AddressFamily inet"'
  paths:
    files: files
    drush-script: drush9
