# Files to include for deployment to the environment in a build artifact.
# The deploy.sh script will sync files and directories from working repo to artifact with:
# rsync -KPar --delete --force --files-from=[path to THIS file] [working repo] [deployment repo]

# One directory of filename per line relative to project root.

#pantheon.yml
config
drush
scripts
tests
vendor
web
composer.json
composer.lock
phpunit.xml.dist
